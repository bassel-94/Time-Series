---
title: "R Notebook"
output: 
default : html_notebook
---

```{r}
rm(list=ls())
if(!require(MTS)) install.packages("MTS")
library(ggplot2)
library(tidyverse)
library(zoo)
```

```{r}
df = read.csv2("data3.csv", header = T, dec = ".", sep = ",") %>% 
  mutate(Date = as.POSIXct(Date))
tail(df)
```
